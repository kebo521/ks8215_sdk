[CHANGELOG] [ +新增 | -删除 | *优化 | #修复 | ~替换 ]

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
【应用接口修改记录】

[V0.5.0]
* 修改公钥更新去掉Hash校验；
* 修改去掉内核提示信息；


[V0.4.9]
~ 更新内核版本到【V43E1.0.2】
# 修复读取终端参数未按指定长度的错误
~ 替换默认状态检查标记为关闭
+ 新增部分操作类的返回值

[V0.4.8]
# 修复4.3E版本的EMV测试若干问题

[V0.4.7]
# 修复终端AID长度为16字节无法识别问题

[V0.4.6]
# 修复哈希运算调用参数问题
~ 更新emv_preprocess接口，允许通过参数设置是否联机优先
~ 更新内核版本到【V43D1.0.26】

[V0.4.5]
+ 新增emv_alloc_buffer接口，允许应用开辟内核的全局内存空间
+ 新增emv_set_issref_result接口，允许应用设置发卡行参考结果
~ 更新内核版本到【V43D1.0.23】

[V0.4.4]
* 优化IAR工程
# 修复emvporting_get_termapps导致程序跑飞的问题

[V0.4.3]
+ 新增emv_set_force_online接口的实现
+ 新增emv_set_account_type_selected和emv_set_offline_pin_entered接口
~ 替换emv_set_offline_pin_entered接口，支持导入密文PIN输入进内核（和原来的不兼容）
* 优化所有请求操作的接口的返回值
~ 替换内核版本为V43D1.0.22

[V0.4.2]
# 修复编译报EMV_Init、EMV_TLV_Decode和EMV_TLV_Find未定义的问题

[V0.4.1]
# 修复JNI编译的库出现错误描述未定义问题
# 修复获取终端应用列表异常问题
~ 替换API接口到2.0版本

[V0.4.0]
* 更新emv_set_termparam为TLV格式导入
* 更新emv_add_aidparam为TLV格式导入
* 更新emv_add_capkparam为TLV格式导入
* 更新错误码调试

[V0.3.0]
* 更新证书失效日期为8个字节
* 分离内核接口和应用接口（优先选应用接口EMVAPI进行应用开发）
+ 新增部分TAG的定义

[V0.1.0]
+ 初始发布


~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
【内核接口修改记录】

[V43E1.0.2]
~ 替换默认状态检查标记为关闭
+ 增加判断终端电子现金支持指示器
* 修改实际应用卡片返回TAG长度为0内容为空的处理；

[V43E1.0.1]
* 优化卡片数据中含9F66的处理
+ 新增EMV_TERMINATE给应用使用
# 修复CVM列表中有两次脱机PIN的处理异常
+ 新增国密算法的支持
* V2CE0030804增加限制TAG87返回1个字节以上的时候应该当作错误处理，不能加入候选列表
* V2CA1240002 的9F22 赋值不是从ICC读记录读取到的8F 的值
# 修复非接联机PIN处理问题
+ 新增非接带脚本的支持

[V43D1.0.28]
# 修复TLV数据中有无效数据的解析问题
# 修复输入脱机PIN次数不准的问题

[V43D1.0.27]
# 修复了联机结果按脱机结果返回的问题

[V43D1.0.26]
# 修复EMV_TLV_PacketByTAG接口长度未输出问题
~ 更新EMV_PreProcess接口，通过参数配置非接是否联机优先 

[V43D1.0.25] 
+ 新增读取电子现金余额的实现
+ 新增简化流程的特殊交易类型<_EMV_TRANSTYPE_LITE_FLOW>

[V43D1.0.24] 
# 修复联机授权响应数据中无ARC返回导致的问题

[V43D1.0.23] 
+ 新增EMV_ICC_Apdu接口
* 移动EMV_ICC_GetData、EMV_ICC_ReadRecord和EMV_GetTransLog接口到emv_utils.h中

[V43D1.0.22] 
# 兼容Android调试内存接口输入不能为NULL
~ 替换联机PIN的错误码[-1603 -> -1602]
+ 新增EMV_TLV_PacketByTAG接口
* 优化部分注释

[V43D1.0.21] 
~ 替换trace_hex接口（同步SDK2.0）
+ 新增EMV_ICC_GetData和EMV_ICC_ReadRecord两个接口

[V43D1.0.20] 
* 更新错误码调试

[V43D1.0.10] 
* 更新证书失效日期为8个字节
* 分离内核接口和应用接口（优先选应用接口EMVAPI进行应用开发）
+ 新增部分TAG的定义